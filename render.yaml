services:
  - type: web
    name: dados-publicos-br
    env: python
    buildCommand: echo "Sem dependencias externas"
    startCommand: python scripts/api_minimal.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /health
