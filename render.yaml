services:
  - type: web
    name: dados-publicos-br
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd scripts && python api_simple.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /health
